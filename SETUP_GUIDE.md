# 湾区活动网站搭建指南

## 📋 你需要做的准备工作

在开始搭建之前，请完成以下准备步骤：

### 1. 账号注册

#### ✅ GitHub 账号 (必需)
- 如果还没有，前往 https://github.com 注册
- 用于代码托管和自动部署

#### ✅ Vercel 账号 (必需 - 免费托管)
- 前往 https://vercel.com
- **直接用 GitHub 账号登录** (推荐)
- 完全免费，无需信用卡

#### ❌ 不需要的账号
- 不需要域名注册商账号 (Vercel 提供免费域名)
- 不需要服务器 (Vercel 提供)
- 不需要数据库服务 (使用现有 SQLite)

### 2. 本地开发环境

#### 确认已安装 Node.js
```bash
node --version  # 应该是 v18 或更高
npm --version   # 应该是 v9 或更高
```

如果没有安装，前往 https://nodejs.org 下载安装 LTS 版本。

#### 确认 Git 已配置
```bash
git --version
git config --global user.name  # 检查是否已设置
```

### 3. 确认现有项目结构

确认你的项目中有以下文件：
```bash
ls -la data/events.db       # SQLite 数据库
ls -la src/                 # 爬虫代码
ls -la .github/workflows/   # GitHub Actions 配置
```

---

## 🚀 搭建步骤

### 阶段 1: 本地初始化 (15-20分钟)

#### 步骤 1.1: 创建 Next.js 项目

在你的项目根目录运行：

```bash
# 进入项目目录
cd /path/to/bay-area-events-scraper

# 创建 Next.js 应用
npx create-next-app@latest website --typescript --tailwind --app --no-src-dir --import-alias "@/*"
```

**交互式问题回答**:
```
✔ Would you like to use ESLint? … Yes
✔ Would you like to use Turbopack? … No
✔ Would you like to customize the import alias? … No
```

#### 步骤 1.2: 安装额外依赖

```bash
cd website

# 安装 SQLite 数据库库
npm install better-sqlite3

# 安装类型定义
npm install -D @types/better-sqlite3
```

#### 步骤 1.3: 创建项目结构

```bash
# 创建必要的目录
mkdir -p lib
mkdir -p public/data
mkdir -p app/components
mkdir -p app/api/events

# 复制数据库文件
cp ../data/events.db public/data/events.db
```

你的目录结构现在应该是：
```
bay-area-events-scraper/
├── src/                    # 现有爬虫代码
├── data/                   # 现有数据库
├── website/                # 新的网站项目 ⭐
│   ├── app/
│   ├── lib/
│   ├── public/
│   │   └── data/
│   │       └── events.db
│   ├── package.json
│   └── next.config.js
└── package.json
```

---

### 阶段 2: 本地开发 (我来帮你写代码)

#### 步骤 2.1: 告诉我开始

**你只需要说**: "好了，开始写代码吧"

我会帮你创建以下文件：

1. **lib/db.ts** - 数据库查询函数
2. **lib/types.ts** - TypeScript 类型定义
3. **app/page.tsx** - 首页
4. **app/components/EventCard.tsx** - 活动卡片
5. **app/components/FilterBar.tsx** - 筛选栏
6. **app/api/events/route.ts** - API 路由
7. **tailwind.config.ts** - 样式配置

#### 步骤 2.2: 本地测试

代码写完后，你运行：
```bash
cd website
npm run dev
```

然后在浏览器打开 http://localhost:3000 查看效果。

---

### 阶段 3: 部署到 Vercel (10分钟)

#### 步骤 3.1: 推送代码到 GitHub

```bash
# 在项目根目录
git add website/
git commit -m "Add Bay Area Events website"
git push origin main  # 或 master，取决于你的主分支名
```

#### 步骤 3.2: 连接 Vercel

1. **登录 Vercel**: https://vercel.com
2. **点击 "Add New Project"**
3. **选择你的 GitHub 仓库**
4. **配置项目设置**:
   ```
   Framework Preset: Next.js
   Root Directory: website
   Build Command: (默认即可)
   Output Directory: (默认即可)
   Install Command: npm install
   ```
5. **点击 "Deploy"**

等待 1-2 分钟，你的网站就上线了！

#### 步骤 3.3: 获取网站地址

部署完成后，Vercel 会给你一个地址，类似：
```
https://your-project-name.vercel.app
```

---

### 阶段 4: 配置自动更新 (5分钟)

#### 步骤 4.1: 更新 GitHub Actions 工作流

我会帮你更新现有的 `.github/workflows/scrape-events.yml`，添加以下步骤：

```yaml
- name: Copy database to website
  run: |
    cp data/events.db website/public/data/events.db
    git add website/public/data/events.db
```

#### 步骤 4.2: 确认自动部署

每周三爬虫运行后：
1. GitHub Actions 更新数据库
2. Git push 触发
3. Vercel 自动检测变更
4. 自动重新部署
5. 30-60秒后网站更新

**完全自动化，无需手动操作！**

---

## 📝 完整时间线

| 阶段 | 任务 | 预计时间 | 你需要做什么 |
|------|------|---------|-------------|
| 准备 | 注册账号 | 5分钟 | 注册 Vercel (用GitHub登录) |
| 准备 | 检查环境 | 2分钟 | 运行 node --version |
| 阶段1 | 创建项目 | 5分钟 | 运行 npx create-next-app |
| 阶段1 | 安装依赖 | 3分钟 | 运行 npm install |
| 阶段1 | 创建目录 | 2分钟 | 运行 mkdir 命令 |
| 阶段2 | 写代码 | 30分钟 | **我来做** |
| 阶段2 | 本地测试 | 5分钟 | 运行 npm run dev |
| 阶段3 | 推送代码 | 2分钟 | git add & commit & push |
| 阶段3 | Vercel部署 | 3分钟 | 点击几个按钮 |
| 阶段4 | 配置自动更新 | 5分钟 | **我来做** |
| **总计** | | **约1小时** | **大部分我来做** |

---

## ✅ 检查清单

在告诉我开始之前，请确认：

### 必需完成 ✅
- [ ] 已有 GitHub 账号
- [ ] 已注册 Vercel 账号 (用 GitHub 登录)
- [ ] 本地已安装 Node.js (v18+)
- [ ] 已运行 `npx create-next-app@latest website`
- [ ] 已安装依赖 `npm install better-sqlite3`
- [ ] 已复制数据库 `cp data/events.db website/public/data/`

### 可选 (后续可做)
- [ ] 自定义域名 (Vercel 免费支持)
- [ ] Google Analytics (流量分析)
- [ ] 设置环境变量

---

## 🎯 下一步

**准备好了吗？**

请告诉我：
1. "准备好了，开始创建 Next.js 项目" - 我会指导你运行命令
2. "已经创建好项目了，开始写代码吧" - 我会直接开始写代码
3. "遇到问题了：[描述问题]" - 我会帮你解决

---

## 💡 常见问题

### Q: 我需要付费吗？
A: **完全不需要**。Vercel 免费套餐足够支撑 10,000+ 月访问量。

### Q: 需要管理服务器吗？
A: **不需要**。Vercel 全自动管理，你只需要 git push。

### Q: 数据怎么更新？
A: **全自动**。每周三 GitHub Actions 运行爬虫，自动更新并部署。

### Q: 网站速度快吗？
A: **非常快**。全球 CDN + 智能缓存，响应时间 < 50ms。

### Q: 出问题了怎么办？
A: **告诉我**，我会帮你解决。Vercel 也有很好的日志和错误提示。

### Q: 可以添加自定义域名吗？
A: **可以**。Vercel 免费支持自定义域名和 HTTPS。

### Q: 未来可以扩展功能吗？
A: **完全可以**。这个架构支持添加 Newsletter、用户登录、API 等功能。

---

## 📚 参考资源

- **Next.js 文档**: https://nextjs.org/docs
- **Vercel 文档**: https://vercel.com/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **本项目设计文档**: 查看 `WEBSITE_DESIGN.md`

---

准备好了就告诉我，我们开始吧！ 🚀
